function init() {
    console.log("followMe @PixelarteCl");
    var a = [ {
        src: "assets/images/pixel.png",
        id: "pixel"
    } ];
    maxHeight = 500, maxWidth = 960, pixelarte = new pixelarteGame({
        idCanvas: "canvas",
        debug: !1,
        width: maxWidth,
        height: maxHeight,
        loop: main.loop,
        fps: 30,
        assets: a,
        assetsPathSound: "assets/sound/",
        assetsSound: []
    }), pixelarte.addEventlistener("assetsLoaderProgress", assetsLoaderProgress), pixelarte.addEventlistener("assetsLoaderComplete", assetsLoaderComplete);
}

function assetsLoaderProgress() {}

function assetsLoaderComplete() {}

function loop() {}

var pixelarte, main = this, maxWidth, maxHeight;

window.onload = init;
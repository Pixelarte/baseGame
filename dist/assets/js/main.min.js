function init() {
    console.log("followMe @PixelarteCl");
    var a = [ {
        src: "assets/images/pixel.png",
        id: "pixel"
    }, {
        src: "assets/images/fondo.jpg",
        id: "fondo"
    } ];
    maxHeight = 500, maxWidth = 960, pixelarte = new pixelarteGame({
        idCanvas: "canvas",
        debug: !0,
        width: maxWidth,
        height: maxHeight,
        loop: main.loop,
        fps: 30,
        assets: a,
        assetsPathSound: "assets/sound/",
        assetsSound: [ {
            src: "loop.mp3",
            id: "loop"
        } ]
    }), pixelarte.addEventlistener("assetsLoaderProgress", assetsLoaderProgress), pixelarte.addEventlistener("assetsLoaderComplete", assetsLoaderComplete), 
    pixelarte.addEventlistener("assetsSoundLoaderComplete", assetsSoundLoaderComplete);
}

function assetsLoaderProgress() {
    console.log(pixelarte.progressLoader);
}

function assetsLoaderComplete() {
    var a = pixelarte.bitmap("fondo", !1);
    pixelarte.Contenedor.addChild(a);
}

function assetsSoundLoaderComplete() {
    pixelarte.soundLoop("loop", 1);
}

function loop() {}

var pixelarte, main = this, maxWidth, maxHeight, contenedorBg;

window.onload = init;
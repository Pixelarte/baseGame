function init() {
    console.log("followMe @PixelarteCl"), pixelarte = new pixelarteGame({
        idCanvas: "canvas",
        stats: !0,
        width: 900,
        height: 600,
        assets: [ {
            src: "assets/images/bg.jpg",
            id: "bg"
        } ]
    }), pixelarte.addEventlistener("assetsLoaderComplete", assetsLoaderComplete);
}

function assetsLoaderComplete() {
    Bg = new createjs.Bitmap(pixelarte.disctionary.get("bg").result), pixelarte.Contenedor.addChild(Bg);
}

var pixelarte, Contenedor;

window.onload = init;